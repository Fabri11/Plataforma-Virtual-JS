// Indicamos cuando tiene que desaparecer el Spinner
window.onload = function(){
    let contenedor = document.getElementById('contenedorSpinner');

    contenedor.style.visibility = 'hidden';
    contenedor.style.opacity = '0';
}